<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}<link rel="stylesheet" href="{{ asset('/css/styles.css') }}">{% endblock %}
    {#(asset)on static files, symfony prefixes every static URL with your CDN host, faster loading times#}
    {#param-converted#}
    </head>
    <body>
    {#{% set user = app.session.get('user') %}#}
    {#{% if user is null %}#}
    {#<p>Login: <a href="{{ path('loginSymf') }}">login</a></p>#}
    {#{% else %}#}
    {#<header>#}
        {#You are logged in: {{ user.username }}#}
        {#<br>#}
        {#<a href="{{ path('logout') }}">logout</a>#}
    {#</header>#}
    {#{% endif %}#}

    {% set lecturer = app.session.get('lecturer') %}
    {% if lecturer is null %}
        <p>you are not logged in: <a href="{{ path('login') }}">login</a></p>
    {% else %}
        <header>
            You are logged in  {{ lecturer.username }}<br>
            <ul><li><a href="{{ path('lecture_index') }}">Lecturers Home page</a></li></ul>
            <a href="{{ path('logout') }}">logout</a>
        </header>
    {% endif %}

    {% block Logout %}{% endblock %}

        <div class="flex-container">
         <header>
            <h1>Bibliography Reference System</h1>
         </header>
            <ul>
                <li><a href="{{ path('homepage') }}">Home page</a></li>
                <li><a href="{{ path('showBibliography') }}">Repository</a></li>
                <li><a href="{{ path('loginSymf') }}">Student Login</a></li>
                <li><a href="{{ path('login') }}">Lecturer Login</a></li>
            </ul>

        {#--------------------------------------------#}

        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}

        {#--------------------------------------------#}

        </div>
    </body>
</html>
