{% extends '_base.html.twig' %}

{% block body %}
    <h1>Studentref2</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ studentRef2.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ studentRef2.name }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ studentRef2.reference }}</td>
            </tr>
            <tr>
                <th>Tag</th>
                <td>{{ studentRef2.tag }}</td>
            </tr>
            <tr>
                <th>Textsummary</th>
                <td>{{ studentRef2.textSummary }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('studentref2_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('studentref2_edit', { 'id': studentRef2.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
